BEGIN;

UPDATE public.questions q
SET options = v.options::jsonb, correct_index = v.correct_index, correct_indices = jsonb_build_array(v.correct_index)
FROM (VALUES
  ('linux-q158', '["シンボリックリンク自体は残るが、開こうとしてもリンク切れになる","シンボリックリンクのファイルも、削除にあわせて自動的に一緒に消える","シンボリックリンクが自動的に別の似た名前のファイルを指すよう更新される","削除は失敗し、元のファイルもシンボリックリンクも両方残り続ける"]', 0),
  ('linux-q168', '["同じキー入力を「命令」か「文字」かで区別する必要があるから","モードを設定しないと、そもそもファイルを開くことができない仕組みになっているから","モードが変わるごとに、使えるキーボードの種類そのものが物理的に切り替わるから","モードは見た目の色を変えるためだけの、機能とは無関係な飾りだから"]', 0),
  ('linux-q172', '["メモリ上で編集していた変更内容を、ディスク上のファイルへ書き込む","画面上に表示されている内容を、印刷用のデータへと変換する","ディスク上のファイルの内容を、いったんすべて画面上に読み込み直す","編集していた内容を、ネットワーク経由で別のコンピュータへ送信する"]', 0),
  ('linux-q173', '["キーボードから手を離さず、修飾キーも押さえずに操作を完結できる","モードを使う方が、覚えるべきキーの組み合わせの総数が常に少なくなる","モードを使う方式は、マウスと組み合わせたときにだけ本領を発揮する","モードを使う方式では、そもそも保存や終了といった操作自体が不要になる"]', 0),
  ('linux-q180', '["ディスクの一部をメモリ代わりに使う「スワップ」に、一部データを退避させる","メモリを多く消費しているプロセスを、確認なくすべて強制的に終了させる","足りない分のメモリを、自動的にネットワーク経由で他のコンピュータから借りてくる","足りなくなった時点で、コンピュータの電源が自動的に落ちるようになっている"]', 0)
) AS v(source_ref, options, correct_index)
WHERE q.source_ref = v.source_ref
  AND q.subject_id = (SELECT id FROM public.subjects WHERE slug = 'linux-basics');

COMMIT;
