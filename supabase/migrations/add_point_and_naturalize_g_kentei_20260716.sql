BEGIN;
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"「中身が分かると急に知能扱いされなくなる」という“人の受け取り方の変化”なら、AI効果。"'::jsonb) WHERE source_ref = 'g-kentei-q1' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"学習せず、決められた動きしかしないなら、制御（レベル1）。"'::jsonb) WHERE source_ref = 'g-kentei-q2' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"「本物の心は無くていい、道具として役立てば十分」が弱いAI。"'::jsonb) WHERE source_ref = 'g-kentei-q3' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"正しく受け答えできても意味は理解していない、と示すのが中国語の部屋。"'::jsonb) WHERE source_ref = 'g-kentei-q4' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"何を無視していいか決められず、考えが無限に広がるなら、フレーム問題。"'::jsonb) WHERE source_ref = 'g-kentei-q5' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"言葉（記号）と、それが指す実物の意味が結びつかないなら、シンボルグラウンディング問題。"'::jsonb) WHERE source_ref = 'g-kentei-q6' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"人がルールを書いて知識を持たせる仕組みなら、エキスパートシステム。"'::jsonb) WHERE source_ref = 'g-kentei-q7' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"まず一本道を深く進み、行き止まったら戻る。これが深さ優先探索。"'::jsonb) WHERE source_ref = 'g-kentei-q8' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"専門家の知識を人手でルールにする作業が重すぎて詰まる。これが知識獲得のボトルネック。"'::jsonb) WHERE source_ref = 'g-kentei-q9' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"相手の言葉をおうむ返しにして会話に見せた元祖プログラムなら、ELIZA。"'::jsonb) WHERE source_ref = 'g-kentei-q10' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"正解ラベルを使わず「まとめる・減らす」なら教師なし。「当てる」なら教師あり。"'::jsonb) WHERE source_ref = 'g-kentei-q11' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"出力を確率に変えてグループ分けするなら分類。名前の「回帰」に惑わされない。"'::jsonb) WHERE source_ref = 'g-kentei-q12' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"k-meansは正解なしのグループ分け（教師なし）、k-NNは近所の正解で多数決（教師あり）。"'::jsonb) WHERE source_ref = 'g-kentei-q13' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"情報をできるだけ保ったまま、項目を少ない軸に減らすなら、PCA。"'::jsonb) WHERE source_ref = 'g-kentei-q14' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"訓練データでは高得点なのに、初見のデータでガタ落ちなら、過学習。"'::jsonb) WHERE source_ref = 'g-kentei-q15' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"データを分け直し、テスト役を交代させて何度も測って平均するなら、交差検証。"'::jsonb) WHERE source_ref = 'g-kentei-q16' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"前の間違いを重視して、学習器を順番に足していくなら、ブースティング。"'::jsonb) WHERE source_ref = 'g-kentei-q17' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"小さな傾きが層の数だけ掛け合わさって信号が消える。これが勾配消失。"'::jsonb) WHERE source_ref = 'g-kentei-q18' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"3つ以上の候補を「合計100%」の確率にそろえるなら、ソフトマックス。"'::jsonb) WHERE source_ref = 'g-kentei-q19' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"Adamは「勢い（モメンタム）」と「歩幅の自動調整（RMSProp）」の合わせ技。"'::jsonb) WHERE source_ref = 'g-kentei-q20' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"誤差を出口から入口へさかのぼらせ、各重みの直し方を求めるなら、誤差逆伝播法。"'::jsonb) WHERE source_ref = 'g-kentei-q21' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"重みをちょうど0にして効く項目だけ残すのがL1。全体を薄く小さくするのがL2。"'::jsonb) WHERE source_ref = 'g-kentei-q22' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"学習のたびに一部のノードをランダムに休ませて過学習を抑えるなら、ドロップアウト。"'::jsonb) WHERE source_ref = 'g-kentei-q23' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"同じフィルタを画像じゅうで使い回し、場所によらず局所の特徴を拾うなら、畳み込み層。"'::jsonb) WHERE source_ref = 'g-kentei-q24' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"特徴マップを縮め、位置が多少ずれても拾えるようにするなら、プーリング。"'::jsonb) WHERE source_ref = 'g-kentei-q25' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"門と記憶で長い情報を保ち、勾配消失に対処するのがLSTM。"'::jsonb) WHERE source_ref = 'g-kentei-q26' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"同じ文の中で、単語どうしの関係の強さを測る仕組みなら、自己注意。"'::jsonb) WHERE source_ref = 'g-kentei-q27' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"系列全体を一度に見て並列に計算できるのが、Transformerの強み。"'::jsonb) WHERE source_ref = 'g-kentei-q28' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"入力を圧縮し、そこから元に戻すよう学ぶなら、オートエンコーダ。"'::jsonb) WHERE source_ref = 'g-kentei-q29' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"物の「種類」と「位置（枠）」を同時に答えるなら、物体検出。"'::jsonb) WHERE source_ref = 'g-kentei-q30' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"画素ひとつずつにラベルを塗り分けるなら、セマンティックセグメンテーション。"'::jsonb) WHERE source_ref = 'g-kentei-q31' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"BERTは前後を見て理解する型、GPTは前から次の語を予想して生成する型。"'::jsonb) WHERE source_ref = 'g-kentei-q32' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"意味の近さが距離に表れる、密なベクトル表現なら、word2vec。"'::jsonb) WHERE source_ref = 'g-kentei-q33' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"作る側と見破る側を競わせて本物そっくりを目指すなら、GAN。"'::jsonb) WHERE source_ref = 'g-kentei-q34' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"ノイズを少しずつ取り除いて画像を作るなら、拡散モデル。"'::jsonb) WHERE source_ref = 'g-kentei-q35' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"転移学習が大きな考え方、ファインチューニングはその中で重みを微調整する具体手法。"'::jsonb) WHERE source_ref = 'g-kentei-q36' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"本開発の前に小さく試し、実現性と効果を見極める工程なら、PoC。"'::jsonb) WHERE source_ref = 'g-kentei-q37' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"データに正解ラベルを人手で付ける作業なら、アノテーション。"'::jsonb) WHERE source_ref = 'g-kentei-q38' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"開発から運用・監視・作り直しまでを継続的に回す実践なら、MLOps。"'::jsonb) WHERE source_ref = 'g-kentei-q39' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"見落とし（見逃し）を何より減らしたいなら、再現率を高める。"'::jsonb) WHERE source_ref = 'g-kentei-q40' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"適合率と再現率のバランスを、1つの数値で見るなら、F値。"'::jsonb) WHERE source_ref = 'g-kentei-q41' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"標準偏差1つ分（±1σ）はおよそ68%。2つ分で約95%、3つ分で約99.7%。"'::jsonb) WHERE source_ref = 'g-kentei-q42' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"判定のさじ加減を変えた曲線がROC、その下の面積がAUC。"'::jsonb) WHERE source_ref = 'g-kentei-q43' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"作品を「味わう」目的でなく、学習の材料として使う利用なら、原則許可なくOK。"'::jsonb) WHERE source_ref = 'g-kentei-q44' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"「秘密管理・有用・非公知」の3つを満たす情報を守るのが、営業秘密。"'::jsonb) WHERE source_ref = 'g-kentei-q45' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"成果物やモデルの権利は、法律でなく契約で決めておく。これが実務。"'::jsonb) WHERE source_ref = 'g-kentei-q46' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"使うときの入力に、人には気づけない小さなノイズを混ぜて誤らせるなら、敵対的サンプル。"'::jsonb) WHERE source_ref = 'g-kentei-q47' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"「なぜその判断か」を、効いた特徴で説明する手法なら、LIMEやSHAP。"'::jsonb) WHERE source_ref = 'g-kentei-q48' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"実在の人物そっくりの偽動画・偽音声を作る技術なら、ディープフェイク。"'::jsonb) WHERE source_ref = 'g-kentei-q49' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || jsonb_build_object('point', '"おすすめが好みの情報ばかり見せ、視野が狭まる状態なら、フィルターバブル。"'::jsonb) WHERE source_ref = 'g-kentei-q50' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"kid": "すごいと思った手品も、タネが分かると「なんだ、そういうことか」と急にありがたみが薄れる。AIでも同じで、中身が知れ渡ると「ただの計算だ」と扱われてしまう。"}'::jsonb WHERE source_ref = 'g-kentei-q1' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"think": "第2次ブームがつまずいた原因は、専門家の知識を人手でルールに書き出す作業が、あまりに大変だったこと。「人手でルール化」がキーワード。"}'::jsonb WHERE source_ref = 'g-kentei-q9' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"asked": "正解ラベルを使わない“教師なし学習”はどれか（あてはまるものをすべて選ぶ）。"}'::jsonb WHERE source_ref = 'g-kentei-q11' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"kid": "たくさんある似た項目を、大事さを保ったまま「少ない軸」にまとめて減らす道具。グループ分けや予測が目的ではない。"}'::jsonb WHERE source_ref = 'g-kentei-q14' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"eg": "料理がまずかった原因を、皿から盛り付け、味付け、下ごしらえへと後ろ向きにたどって、どの工程を直せばいいか突き止めるようなもの。"}'::jsonb WHERE source_ref = 'g-kentei-q21' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"kid": "L1は要らない重みを「スパッと0」にして、効く項目だけ残す（＝項目のえり分け）。L2は重み全体を「なだらかに小さく」するだけで、ちょうど0にはしにくい。"}'::jsonb WHERE source_ref = 'g-kentei-q22' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"think": "RNNは前から一語ずつしか進めず遅い。対してTransformerは文全体を一度に見て並列に計算できる。この並列化が最大の利点。"}'::jsonb WHERE source_ref = 'g-kentei-q28' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
UPDATE public.questions SET explanation_data = explanation_data || '{"eg": "料理は作って終わりではなく、お客の評判を見てレシピを改良し、また出す。それを店として回し続けるような仕組み。"}'::jsonb WHERE source_ref = 'g-kentei-q39' AND subject_id = (SELECT id FROM public.subjects WHERE slug='g-kentei');
COMMIT;
