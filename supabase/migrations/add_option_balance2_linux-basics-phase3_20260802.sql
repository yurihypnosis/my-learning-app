BEGIN;

UPDATE public.questions q
SET options = v.options::jsonb
FROM (VALUES
  ('linux-q118', '["アーカイブを戻す作業を展開(または解凍)、圧縮を戻す作業も展開(または解凍)と呼ぶことが多い","アーカイブを戻す作業のことを圧縮と呼び、圧縮を戻す作業のことをアーカイブと呼ぶことが多い","アーカイブと圧縮を戻す作業には、それぞれ二度と元に戻せない専用の名前しか用意されていない","一度アーカイブ・圧縮したファイルは、どんな手順を踏んでも二度と元の状態には戻せない"]'),
  ('linux-q120', '["カーネルに加え、パッケージ管理の仕組みや初期設定などを組み合わせて配布用にまとめたもの","ハードウェアの部品そのものを、製造したメーカーごとに仕分けしてまとめて配布したもの","特定のプログラミング言語だけを、用途に関わらずかき集めてひとまとめに配布したもの","インターネット回線の契約プランを、提供する事業者ごとに仕分けしてまとめて配布したもの"]'),
  ('linux-q123', '["カーネルは共有していても、パッケージ管理の仕組みや周りの道具はディストリビューションごとに異なるため","コンテナごとに、共有しているはずのカーネルそのものを個別にインストールし直す必要があるため","ディストリビューション名は見た目だけのラベルにすぎず、中身の動作にはまったく影響しないため","ベースイメージの選択は、コンテナの起動速度とはまったく無関係な、単なる好みの問題でしかないため"]')
) AS v(source_ref, options)
WHERE q.source_ref = v.source_ref
  AND q.subject_id = (SELECT id FROM public.subjects WHERE slug = 'linux-basics');

COMMIT;
