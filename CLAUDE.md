# my-learning-app

Yuu 個人の学習アプリ。G検定・統計・Linux・各種資格の図解ドリルを問題集として蓄積している。詳しい作問ルールは `.claude/skills/question-authoring*` を参照。

## 標準ワークフローへの標準許可（確認なしで進めてよいこと）

このリポジトリでの日常的な作業フローについては、都度「コミットしていいですか」「pushしていいですか」と確認を挟まなくてよい。以下は既に本人から包括的に許可されている。**Linux・G検定に限らず、今後追加される新しい問題集ドメイン（新しい資格・新しい図解ドリルなど）にも同じルールが自動的に適用される** — 新ドメインだからといって都度この許可を取り直さなくてよい:

- **問題集・スキルファイルのコミットとpush。** `supabase/migrations/*.sql`（作問・DB適用済みの内容）、`.claude/skills/*` の追加・更新は、作業が一段落した時点でコミットして `main` に push してよい（Vercelへの自動デプロイもこの一部）。単発の確認は不要
- **本番DBへの適用。** `.claude/skills/question-authoring/scripts/db.sh apply` によるSupabase本番反映は、この問題集ワークフローの一部として確認なしで実行してよい（[[project_prod_db_write_path]]参照）。**新しいsubject（新しい問題集そのもの）の作成も含む** — 既存subjectへの追加だけでなく、ゼロから新しいドリルを立ち上げる場合も同様に確認不要
- **新しい `question-authoring-<ドメイン名>` スキルの新規作成。** 新しい問題集ドメインを始めるときにスキルファイル自体を新規に書き起こすことも、上記「スキルファイルの追加・更新」に含まれる。作ってよいか確認する必要はない
- **viz図解の検証に伴う一時ファイル操作。** SVGをPNG化して目視確認する作業（`qlmanage` 等）や、検証後の一時PNG/HTMLファイルの削除（`rm -f *.png` 等、作業ディレクトリ内の生成物に限る）は確認不要
- **バックグラウンドエージェント（fork/サブエージェント）への作問・DB適用・コミット/pushの委任。** このワークフロー一式をエージェントに任せること自体、都度の確認は不要
- **軽微な判断は聞かずに進めてよい。** 作業中に見つかった、今のタスクと無関係な未使用ファイル・半端な状態のファイル（例: 中身が空のまま残っている `src/features/quiz/lib/streak.ts` のような未使用スタブ）は、削除などの実際の変更はせずそのまま放置してよい。「削除していいですか」と聞かずに、気づいたことだけ一言メンションすれば十分

2026-08-02、ユウから複数回にわたり「今回許可を求めたことは全部デフォルトで許可するので、以降は聞かないでほしい」という明示の指示があった。上記はその指示を、個別の事例だけでなく**今後起こりうる同種の作業一般**に一般化して反映したもの。新しいエージェント/セッションがこのルールの対象か迷った場合の判断基準は「問題集コンテンツの追加・更新・DB反映という定型ワークフローの一部か」— それであれば対象、それ以外（下記の除外リスト、または全く新しい種類のリスク）であれば通常通り確認する。

**このルールが及ばないもの**（引き続き通常通り確認する）: force push、`git reset --hard` 等の破壊的操作、既存コミットの書き換え、`src/` 配下のアプリケーションコード（画面・ロジック・`RichExplanation` の描画コードなど）に対する変更、ファイルの実削除、このリストに無い新しい種類のリスクの高い操作。迷ったら確認する。

## 参照

- 作問共通ルール: `.claude/skills/question-authoring/SKILL.md`
- 試験別ルール: `.claude/skills/question-authoring-*/SKILL.md`
- 本番DB書き込み経路: memory の `project_prod_db_write_path.md`
